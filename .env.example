# Port the server runs on (optional, defaults to 5000)
PORT=5000

# Only needed if you plug testLLM.js / a real Gemini call into the triage engine.
# Not required for the app to run — the built-in rule-based triage engine works without it.
GEMINI_API_KEY=your_gemini_api_key_here
